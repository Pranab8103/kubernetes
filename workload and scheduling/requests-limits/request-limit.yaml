apiVersion: v1
kind: Pod
metadata:
  name: promethium-pod
spec:
  containers:
  - name: promethium-container
    image: nginx
    resources:
      requests:
        memory: "64Mi"
        cpu: "0.5"
      limits:
        memory: "128Mi"
        cpu: "1"